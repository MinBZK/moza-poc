---
pagination:
  data: regelgevingData
  size: 1
  alias: regeling
permalink: "/moza/regelgeving/{{ regeling.id }}/"
eleventyComputed:
  title: "MijnOverheid Zakelijk: {{ regeling.titel }}"
subNavActive: "/moza/regelgeving/"
---

{% include "side-nav-overheid.njk" %}

<article id="hoofd-inhoud">
	<nav class="breadcrumb">
		<ol>
			<li><a href="{{ '/moza/' | url }}">Home</a></li>
			<li><a href="{{ '/moza/regelgeving/' | url }}">Wetten en regelgeving</a></li>
			<li aria-current="page">{{ regeling.titel }}</li>
		</ol>
	</nav>

	<h1>{{ regeling.titel }}</h1>

	<a href="{{ '/moza/regelgeving/' | url }}" class="back-link">Terug naar Wetten en regelgeving</a>

	<section class="card">
		<p class="intro">{{ regeling.beschrijving }}</p>

		<dl class="data-overview">
			<dt>Bron</dt>
			<dd>{{ regeling.bron }}</dd>
			<dt>Inwerkingtreding</dt>
			<dd>{{ regeling.inwerkingtreding }}</dd>
			<dt>Geldt voor</dt>
			<dd>{{ regeling.geldtVoor }}</dd>
		</dl>

		{% for alinea in regeling.inhoud %}
		<p>{{ alinea }}</p>
		{% endfor %}

		{% if regeling.assistentVraag and regeling.regelrechtRegel %}
		{# Zichtbaar als de informatieplicht geldt: personas.js haalt de drempel
		   pre-chat op uit RegelRecht (GET /regelrecht/definities?law=…) en
		   vergelijkt die met het verbruik van de actieve persona. #}
		<div data-persona-energiedrempel data-regelrecht-law="{{ regeling.regelrechtRegel }}" hidden>
			<p>De digitale assistent helpt je de melding in één keer goed in te dienen.</p>
			<a class="btn-cta" href="{{ '/moza/digitale-assistent/' | url }}?vraag={{ regeling.assistentVraag | urlencode }}">Start met de digitale assistent</a>
		</div>
		{% endif %}

		{% include "action-group.njk" %}

		<div class="action-group">
			<a class="btn-cta" href="{{ regeling.externUrl }}" rel="external">Bekijk op de website van wetten.overheid.nl</a>
		</div>
	</section>
</article>
