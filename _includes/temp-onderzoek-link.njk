{# TIJDELIJK — onderzoekslink juli 2026.
   Verwijderen: haal de include-regels weg (base.njk, mailbox-base.njk, mox/*.html) en delete dit bestand.
   Bewust self-contained met inline styles, niet verweven met style.css.
   Niet tonen op de onderzoekspagina zelf (dat is de bestemming). #}
{% if '/ux-onderzoek/' not in (page.url or '') %}
<style>
	.temp-onderzoek-link {
		background: #333;
      border: 1px solid #111;
		border-end-end-radius: 0.25rem;
		border-start-end-radius: 0.25rem;
		color: #fff;
		font-family: -apple-system, BlinkMacSystemFont, segoe ui, Ubuntu, roboto, noto, sans-serif;
      font-size: 1rem;
      font-weight: bold;
		inset-block-end: 0;
		inset-inline-start: 0;
		padding: 1rem 2rem 1.5rem;
		position: fixed;
		text-decoration: none;
		z-index: 9;
      opacity: .5;
      transition: all .15s;
	}
	.temp-onderzoek-link:hover,
	.temp-onderzoek-link:focus-visible {
		color: #fff;
      opacity: 1;
	}
</style>
<a class="temp-onderzoek-link" href="{{ '/ux-onderzoek/juli-2026/' | url }}">← Takenoverzicht</a>
{% endif %}
